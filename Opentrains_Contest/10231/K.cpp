#include <bits/stc++.h>

using namespace std;

const int N = 1010;

typedef vector<double> po;

int n, d;

po a[N];

vector <int> ans;

int main() {
	ios::sync_with_stdio(false);
	cin >> n >> d;
	for (int i = 0; i < n; i ++) {
		a[i].resize(d);
		for (int j = 0; j < d; j ++)
	}
	cout << (ans.size()) << endl;
	for (int i : ans)
}