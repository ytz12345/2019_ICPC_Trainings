#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int t, n, c;

ll a[N], b[N];

int main() {
	ios::sync_with_std(false);
	for (cin >> t; t --; ) {
		cin >> n >> c;
		for (int i = 1; i <= n; i ++)
			cin >> a[i] >> b[i];
	}

	return 0;
}